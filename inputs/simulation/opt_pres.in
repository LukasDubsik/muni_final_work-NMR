&cntrl

 imin=0,    !Runs the simulations, 1 means optimilization
 nstlim=1000000,     !Run for 1ns
 dt=0.001,      !Units for nstlim
 
 ntb=2,     !NTP system
 cut=9.0,   !PME cutoff (for forces calculation cutoff)
 ntc=2,     !Hydrogen bonds are flexible
 ntf=2,     !Don't skip hydrogen bond computation

 irest=1,   !Continue from previous data
 ntx=5,     !Take over the positions and speeds

 ntp=1,     !Berendsen pressure
 pres0=1.0, !The goal pressure
 taup=5.0,  !Pressure coupling constant - how long till pressure assumes final result
 barostat=2,!The barostat type

 ntt=3,     !Langevin thermostat model
 temp0=300.0,   !Stable temperature
 gamma_ln=2.0,  !Collision frequency
 ig=-1,     

 ntpr=500,      !When to take energy measurements
 ntwx=10000,     !Trajectory
 ntwr=1000,     !Restart

 iwrap=0,
 ioutfm=1,
 ntxo=2,

&end
