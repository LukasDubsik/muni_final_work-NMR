###Script describing the NMr simulation for the cysteone zwitterion in water enviroment (ph ~7.2-7.4)
##No spaces are expected between ':='

##The name of the starting mol2/rst7-parm7 files, will also be used to name other files during the simulation
name:=cys
save_as:=cys_crest_md_long_only_cosmo_split_5_20ns_averaged

##General option on how the job is run
#Check that all necessary files and librariues are present on the system and user can access them
check_modules:=false

##Type of input -> mol2 means that charges should be computed, 7 means parm7/rst7 already provided
input_type:=mol2

##Do we want to leave out gpu speed increase?
gpu:=true

##If we are running in metacentrum
meta:=true

##Directory for metacentrum - where this directory is present (only if meta set to true)
directory:=/storage/brno2/home/lukasdubsik/muni/final_work/novel

##The Amber version we are using (metacentrum has may and not all are available to all)
amber:=/22.1.3-gcc-10.2.1-man1

##Path to a mamba conda enviroment in which crest is installed
mamba:=/storage/brno2/home/lukasdubsik/.conda/envs/crest

##Names for the expected *.in files for individual optimizations
tleap:=tleap
opt_water:=opt_water
opt_all:=opt_all
opt_temp:=opt_temp
opt_pres:=opt_pres
md:=md_long
cpptraj:=cpptraj

##Number of iterations for the md (please set the cpptraj so that correct number of sampling is done)
md_iterations:=5

##Additional parameters to be added when running specific programs (Some can be left if not run, otherwise error, eg. no antechamber when 7 specified in 'input_type')
##The parameters must be enclosed in "..." brackets
#Standart values :- antechamber -i $NAME.mol2 -fi mol2 -o $NAME.mol2 -fo mol2 ($NAME refers to general file naming dependent on the pipeline's progress)
antechamber:=-c bcc -nc 0 -at gaff
#Standart values :- parmchk2 -i $NAME.mol2 -f mol2 -o $NAME.frcmod
parmchk2:= 
