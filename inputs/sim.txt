###Script describing the NMr simulation for the cysteine zwitterion in water environment (ph ~7.2-7.4)
##No spaces are expected between ':='

##The name of the starting mol2/rst7-parm7 files, will also be used to name other files during the simulation
name:=sel_s
save_as:=sel_s_sh

##General option on how the job is run
#Check that all necessary files and libraries are present on the system and user can access them
check_modules:=false

##Type of input -> mol2 means that charges should be computed, 7 means parm7/rst7 already provided
#mol2 - Expected file is just name.mol2 in inputs/structures
input_type:=mol2

##If to use specific force fields params -> yes/no
#If yes, expected leaprc/gaff.zf inside inputs/param dir
params:=yes

#How many gauss frames to aberage through
num_frames:=100

#The overall charge of the molecule
charge:=-1

##Do we want to run with gpu?
gpu:=true

##Mode of cpptraj selection
#bonded - select water h-bonded to the solvent for the qm
#         Expects that your cpptraj script will return frames.nc file
#no_water - only solvent is considered
cpptraj_mode:=no_water

##What is the sigma shift value for the nmr
sigma:=32.2

#Path to a mamba conda environment in which crest along with other necesseray python packages are installed
mamba:=/storage/brno2/home/lukasdubsik/.conda/envs/crest

##If we are running in metacentrum
meta:=true

#Directory for metacentrum - where this directory is present (only if meta set to true)
directory:=/storage/brno2/home/lukasdubsik/muni/final_work/novel
#The Amber version we are using (metacentrum has may and not all are available to all)
amber:=/22.1.3-gcc-10.2.1-man1

##Names for the expected *.in files for individual optimizations
tleap:=tleap_spec
opt_water:=opt_water
opt_all:=opt_all
opt_temp:=opt_temp
opt_pres:=opt_pres
md:=md
cpptraj:=cpptraj

##Number of iterations for the md (please set the cpptraj so that correct number of sampling is done)
md_iterations:=5

##Additional parameters to be added when running specific programs (Some can be left if not run, otherwise error, eg. no antechamber when 7 specified in 'input_type')
##The parameters must be enclosed in "..." brackets
#Standart values :- antechamber -i $NAME.mol2 -fi mol2 -o $NAME.mol2 -fo mol2 ($NAME refers to general file naming dependent on the pipeline's progress)
antechamber:=-c bcc -at gaff2
#Standart values :- parmchk2 -i $NAME.mol2 -f mol2 -o $NAME.frcmod
parmchk2:= 

##What resulting hydrogens to filter
#none -> All hydrogens of the molecule
#alpha_beta -> Only alpha beta hydrogens of the molecule (only if amino acid)
filter:=alpha_beta